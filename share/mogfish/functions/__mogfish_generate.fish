# __mogfish_generate — generate a mog script for novel input
#
# Placeholder: will be implemented when generation pipeline is wired up.
#
# See docs/plans/mogfish-outside-in-tdd.md, Layer 6

function __mogfish_generate --argument-names cmd
    echo "mogfish: would generate script for '$cmd' (not yet implemented)"
end
